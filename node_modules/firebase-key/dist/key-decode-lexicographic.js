"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var key_alphabet_1 = require("./key-alphabet");
function decodeLexicographic(key) {
    var value = 0;
    var lengthChar = key.charAt(0);
    if (lengthChar > "_") {
        for (var k = 1; k < key.length; k++) {
            value *= key_alphabet_1.alphabet.length;
            value += key_alphabet_1.alphabet.indexOf(key.charAt(k));
        }
    }
    else {
        for (var k = 1; k < key.length; k++) {
            value *= key_alphabet_1.alphabet.length;
            value += key_alphabet_1.alphabet.length - 1 - key_alphabet_1.alphabet.indexOf(key.charAt(k));
        }
        value = -value;
    }
    return value;
}
exports.decodeLexicographic = decodeLexicographic;
