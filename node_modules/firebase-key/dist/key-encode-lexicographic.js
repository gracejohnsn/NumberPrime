"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var key_alphabet_1 = require("./key-alphabet");
function encodeLexicographic(value) {
    var result = "";
    if (value === 0) {
        result = "a-";
    }
    else if (value > 0) {
        while (value > 0) {
            var digit = (value % key_alphabet_1.alphabet.length);
            result = key_alphabet_1.alphabet.charAt(digit) + result;
            value -= digit;
            value /= key_alphabet_1.alphabet.length;
        }
        var prefix = key_alphabet_1.alphabet.charAt(result.length + 37);
        result = prefix + result;
    }
    else {
        value = -value;
        while (value > 0) {
            var digit = (value % key_alphabet_1.alphabet.length);
            result = key_alphabet_1.alphabet.charAt(key_alphabet_1.alphabet.length - 1 - digit) + result;
            value -= digit;
            value /= key_alphabet_1.alphabet.length;
        }
        var prefix = key_alphabet_1.alphabet.charAt(37 - result.length);
        result = prefix + result;
    }
    return result;
}
exports.encodeLexicographic = encodeLexicographic;
