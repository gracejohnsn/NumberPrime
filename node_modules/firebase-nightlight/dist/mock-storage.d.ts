import { firebase } from "./firebase";
export interface MockStorageOptions {
    app: firebase.app.App;
    url?: string;
}
export declare class MockStorage implements firebase.storage.Storage {
    private app_;
    constructor(options: MockStorageOptions);
    readonly app: firebase.app.App;
    readonly maxOperationRetryTime: number;
    readonly maxUploadRetryTime: number;
    bucket(name?: string): any;
    ref(path?: string): firebase.storage.Reference;
    refFromURL(url: string): firebase.storage.Reference;
    setMaxOperationRetryTime(time: number): any;
    setMaxUploadRetryTime(time: number): any;
}
