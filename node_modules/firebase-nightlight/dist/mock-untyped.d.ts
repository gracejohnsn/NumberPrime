import { MockIdentity, MockValue } from "./mock-types";
export interface MockOptions {
    apps?: {
        [key: string]: {
            database?: {
                content: MockValue | null;
            };
            identities?: MockIdentity[];
        };
    };
    database?: {
        content: MockValue | null;
    };
    identities?: MockIdentity[];
}
export declare class MockUntyped {
    private apps_;
    private options_;
    constructor(options?: MockOptions);
    readonly apps: any[];
    readonly SDK_VERSION: string;
    app(name?: string): any;
    auth(app?: any): any;
    database(app?: any): any;
    initializeApp(options: any, name?: string): any;
    messaging(app?: any): any;
    storage(app?: any): any;
}
