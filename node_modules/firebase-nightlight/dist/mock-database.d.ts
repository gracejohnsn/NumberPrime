import { firebase } from "./firebase";
import { MockEmitters, MockValue } from "./mock-types";
export interface MockDatabaseOptions {
    app: any;
    database: {
        content: MockValue | null;
    };
    emitters: MockEmitters;
}
export declare class MockDatabase implements firebase.database.Database {
    private app_;
    private database_;
    private emitters_;
    constructor(options: MockDatabaseOptions);
    readonly app: firebase.app.App;
    goOffline(): void;
    goOnline(): void;
    ref(path?: string): firebase.database.Reference;
    refFromURL(url: string): firebase.database.Reference;
}
